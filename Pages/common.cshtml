@page
@model WebApplication1.Pages.commonModel
@{
}
<h1>COMMON PAGE</h1>

@{
    if(TempData["message"]!=null)
    {
        <div>
            <p>Click here to login</p>

            <script>alert("@TempData["message"]")</script>
        </div>
    }
}
@{
    if (TempData["login"] != null)
    {<div>
       
        <script>alert("@TempData["login"]")</script>
        </div>
    }
}
@{
    if (TempData["admin"] != null)
    {
        <div>

            <script>alert("@TempData["admin"]")</script>
        </div>
    }
}

@{
    if(TempData["login"]!=null)
    {
        <h3>@TempData["login"]</h3>
    }
}

@{
    if (TempData["message"] != null)
    {
        <button> <a class="nav-link text-dark" asp-area="" asp-page="/Login">Login</a> </button>
    }
    else
    {
        <button> <a class="nav-link text-dark" asp-area="" asp-page="/Index">Home</a> </button>
    }
}



@* @page
@model WebApplication1.Pages.commonModel
@{
}

<div class="container vh-100 d-flex justify-content-center align-items-center">
    <div class="text-center">
        <h1 class="mt-4">COMMON PAGE</h1>

        @if (TempData["message"] != null)
        {
            <div>
                <p>Click here to login</p>

                <script>alert("@TempData["message"]")</script>
            </div>
        }

        @if (TempData["login"] != null)
        {
            <h3 class="mt-2">@TempData["login"]</h3>
        }

        <div class="mt-3">
            @if (TempData["message"] != null)
            {
               
                <button class="btn btn-primary">
                    <a class="nav-link text-white" asp-area="" asp-page="/Login">Login</a>
                </button>
            }
            else
            {
               
                <button class="btn btn-secondary">
                    <a class="nav-link text-white" asp-area="" asp-page="/Index">Home</a>
                </button>
            }
        </div>
    </div>
</div>
 *@